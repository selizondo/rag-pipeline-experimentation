# llm-utils provider-agnostic LLM config
# Generation calls
LLM_API_KEY=sk-...
LLM_MODEL=gpt-4o-mini
LLM_BASE_URL=https://api.openai.com/v1

# Judge calls (defaults to generation values if unset)
LLM_JUDGE_MODEL=gpt-4o-mini
